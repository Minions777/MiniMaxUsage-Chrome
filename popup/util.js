// MiniMax Token Monitor - popup/util.js
// Thin re-export from lib/utils.js so existing module references continue
// to work. The actual implementations live in lib/utils.js (testable under
// vitest with no DOM dependencies).

(function () {
  'use strict';
  // PMM.util is already populated by lib/utils.js (loaded before this file
  // in popup.html). Nothing to do here; this file is kept for layout
  // stability and to make module boundaries obvious.
})();